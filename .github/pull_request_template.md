## What changed and why

## Checklist

- [ ] `./gradlew :flaglens:testDebugUnitTest` passes
- [ ] `./gradlew :flaglens:lintDebug` passes
- [ ] `./gradlew :flaglens:assembleDebug :sample:assembleDebug` passes
- [ ] README updated if public API or behavior changed
- [ ] No secrets, tokens, or keystores are included in this diff
- [ ] Any change to override gating (`FlagLensConfig.enabled`/`allowLocalOverrides`) is
      privacy/safety-reviewed (see SECURITY.md)
